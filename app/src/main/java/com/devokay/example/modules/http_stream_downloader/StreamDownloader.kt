package com.devokay.example.modules.http_stream_downloader

class StreamDownloader {
//  val limiter = TokenBucketLimiter(128 * 1024) // 128 KB/s
//  limiter.reset()
//
//  val request = DownloadRequest(
//    url = "http://example.com/big.zip",
//    rangeStart = localFile.length(),
//    enableResume = true
//  )
//
//  val call = okHttpDownloader.newCall(request)
//  val response = call.execute()
//
//  val throttledStream = ThrottledInputStream(response.inputStream, limiter)
//
//  FileOutputStream(localFile, request.enableResume).use { out ->
//    throttledStream.copyTo(out)
//  }

}
